machine:
  node:
    version: 0.12
dependencies:
  pre:
    - npm install -g bower
    - npm rebuild node-sass
  override:
    - npm i
    - bower i
  cache_directories:
    - "./node_modules"
    - "./bower_components"
test:
  post:
    - npm test
