<div>
  {{input value=note.title}}
  <button class="btn btn-primary" {{action 'saveNote'}}>save</button>
  <button class="btn btn-primary" {{action 'closeNote'}}>close</button>
</div>
<div>
  {{textarea rows=10 cols=60 value=note.body}}
</div>
<div>
  {{markdown-to-html markdown=note.body}}
</div>
<div>
  {{yield}}
</div>
